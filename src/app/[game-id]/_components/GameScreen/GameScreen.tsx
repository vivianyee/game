import { GameWebSocket } from "@/types/game";

type Props = {
  gameData: GameWebSocket;
};

export default function GameScreen({ gameData }: Props) {
  return (
    <div>
        
    </div>
  );
}
